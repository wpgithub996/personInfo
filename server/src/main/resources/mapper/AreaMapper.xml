<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.wang.server.mapper.AreaMapper">
    <select id="findarea" resultType="com.wang.server.pojo.Area">
        select  areaid,areaname from area where parentid=#{parentid}
    </select>
</mapper>